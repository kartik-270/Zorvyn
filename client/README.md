Finance dashboard frontend (Next.js).

## Getting Started
Create `client/.env.local`:

```bash
NEXT_PUBLIC_API_URL=http://localhost:4000
```

Run:

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

Seeded login accounts are documented in the root `README.md`.
